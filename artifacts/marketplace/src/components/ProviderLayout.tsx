// ProviderLayout is now a thin re-export of the unified DashboardLayout.
// All provider dashboard pages continue to import ProviderLayout unchanged.
export { default } from "@/components/DashboardLayout";
